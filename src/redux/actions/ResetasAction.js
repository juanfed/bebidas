import {useState} from "react";

export const recetasAction = () =>{
    // eslint-disable-next-line react-hooks/rules-of-hooks
    const [busqueda, setBusqueda] = useState({
        ingrediente: '',
        categoria: ''
    })
    return (dispatch) =>{
        
    }
}